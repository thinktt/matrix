events {}

http {
    server {
        listen 80;

        location / {
            root /usr/share/nginx/html;  # NGINX serves files from this directory
            index index.html index.htm;
        }
    }
}

